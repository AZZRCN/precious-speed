# Submission #391858 — mul — AC 21 ms

- 日期：2026/8/10 23:17:21
- 题：Multiplication of Hex Big Integers
- 语言：C++23
- 状态：**AC**
- **headline = 21 ms**（计分点 = `large_02`）
- 峰值内存：30.83 MiB（`fft_killer_01`）
- 版本：**v10c**（原主人确认）；源码快照  已存

## 与上一次 mul 提交的对照

| | #391746 (v5) | **#391858** | Δ |
|---|---|---|---|
| headline | 38 ms | **21 ms** | **−44.7%** |
| 计分点 | max_max_06 | **large_02** | **已转移** |
| 峰值内存 | 33.06 MiB | 30.83 MiB | −6.7% |

## 逐点回执原文

| Name | Status | Time | Memory |
|---|---|---|---|
| example_00 | AC | 1 ms | 0.52 MiB |
| small_00 | AC | **17 ms** | 11.68 MiB |
| medium_00 | AC | 6 ms | 13.27 MiB |
| medium_01 | AC | 5 ms | 14.77 MiB |
| medium_02 | AC | 6 ms | 15.51 MiB |
| large_00 | AC | 11 ms | 14.05 MiB |
| large_01 | AC | 14 ms | 16.01 MiB |
| large_02 | AC | **21 ms** ← headline | 13.80 MiB |
| max_max_00 | AC | 16 ms | 30.03 MiB |
| max_max_01 | AC | 16 ms | 30.18 MiB |
| max_max_02 | AC | 16 ms | 30.25 MiB |
| max_max_03 | AC | 16 ms | 30.79 MiB |
| max_max_04 | AC | 16 ms | 30.02 MiB |
| max_max_05 | AC | 16 ms | 30.50 MiB |
| max_max_06 | AC | 17 ms | 30.79 MiB |
| max_max_07 | AC | 16 ms | 29.51 MiB |
| zero_00 | AC | 1 ms | 4.76 MiB |
| power_00 | AC | 6 ms | 15.29 MiB |
| power_01 | AC | 5 ms | 13.54 MiB |
| fft_killer_00 | AC | 16 ms | 28.84 MiB |
| fft_killer_01 | AC | 17 ms | 30.83 MiB |
| large_small_00 | AC | 15 ms | 20.64 MiB |

---

## ★ 本次回执的最大价值：**计分点已从 max_max 转移到 large_02**

### 本地 vs LC 逐点对照（本地 = VM 上 `v10c_u`，`perf stat -r 3` task-clock）

| 测试点 | 本地 ms | **LC ms** | 比值 | 备注 |
|---|---|---|---|---|
| medium_00 | 5.87 | 6 | 1.02 | 正常 |
| power_01 | 4.57 | 5 | 1.09 | 正常 |
| medium_02 | 4.70 | 6 | 1.28 | 正常 |
| power_00 | 4.63 | 6 | 1.30 | 正常 |
| large_00 | 6.82 | 11 | 1.61 | 偏高 |
| max_max_01 | 9.11 | 16 | 1.76 | 基准偏差 |
| large_small_00 | 8.19 | 15 | 1.83 | |
| fft_killer_01 | 8.80 | 17 | 1.93 | |
| large_01 | 6.93 | 14 | **2.02** | 异常 |
| small_00 | 6.73 | 17 | **2.53** | 异常 |
| **large_02** | **6.65** | **21** | **3.16** | **最异常** |

### 结论（硬数据，非推测）

1. **本地 large_00/01/02 三点几乎无差（6.82 / 6.93 / 6.65 ms），LC 上却是 11 / 14 / 21 ms 单调递增。**
   长度分布也几乎相同（都是 33–35 case、总量 ~3.15 M hex、单操作数 1 K–100 K）。
   ⇒ **本地数据无法复现这个悬崖。** 要么 LC 的 large_02 内容与本地 gen 的不同，
   要么存在数据相关的性能悬崖（长度组合触发坏档位）。

2. **max_max 已经不是瓶颈了。** 八个 max_max 点全部稳定在 16–17 ms，
   而 headline 是 21 ms 的 large_02。**继续在 max_max 上抠 1% 对 LC 得分贡献为零。**

3. **`small_00` = 159 984 个 case**（本地实测 T 值），单 case 极小。
   17 ms 里主要是 **per-case 固定开销 × 16 万次**，不是 FFT。这是一条独立的优化战线。

4. 本地→LC 比值散布在 1.02 – 3.16，**没有统一修正系数**，
   再次坐实 archive/README 的推论 1（偏差是工作集与数据形态的函数）。

---

## 待办

- [x] ~~确认源码版本 + 补存快照~~ → **v10c**，快照 `391858_mul.cpp` 已存（23:34）
- [ ] 复现 large_02 悬崖：扫描 (a_len, b_len) 组合找 ms/理论值 异常点
- [ ] `small_00` per-case 固定开销拆解（16 万次调用的启动成本）
