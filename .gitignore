# Compiled binaries / 编译产物
*.exe
*.o
*.obj

# Test artifacts / 测试产物 (运行时生成, 可重新生成)
logs/
testdata/

# Python cache / Python 缓存
__pycache__/
*.pyc

# IDE / 编辑器
.vscode/
.idea/

# Build directories / 构建目录
benchmark_*/

# OS / 操作系统
Thumbs.db
desktop.ini

# 临时大文件 / 临时大文件
*.tmp
*.bak

# 隐私文件 / Privacy (含 VM 凭据，禁止提交)
ssh_manager.py
scripts/
!scripts/.gitkeep

# 无关项目探索记录 / Unrelated project exploration
exploration/

# 临时日志和记录 / Temp logs and notes
2026年*.md
*.log
moptm_vec.log

# Winbench 大数据文件 / Large test data
winbench/*.in
winbench/*.csv

# AAMP 压缩产物参考（含 VM 路径，untracked）
shrunk_ADD_v4_ref.cpp

# 临时目录 / Temp directories
temp/

# Agent memory / 不提交
.workbuddy/

# 大体积可再生数据 / bulk regenerable data (test inputs, run dumps, artifacts)
*.in
*.ans
*.out
*.dll
*.tgz
*.s
cases/
cases_lf/
bench_data/
